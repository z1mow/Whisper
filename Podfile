# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Whisper' do
  use_frameworks!

  pod 'FirebaseCore'  
  pod 'FirebaseAuth'  
  pod 'FirebaseDatabase'  
  
end
